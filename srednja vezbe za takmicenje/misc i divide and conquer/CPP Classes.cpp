#include<iostream>
#include<list> // [] .erase(), .sort(), .unique(), pop_front(), .pop_back(), .clear(), 
#include<stack> // .push(), pop(), 
#include<queue> // .push, .pop(),  
#include<vector> // [] .assign() for assigning array and how much to be assigned, .reverse(), .erase(), .unique(),
// at() for using position return value,  .pop_back and front, insert() for inserting on location one value or more based on choise or whole range 
using namespace std;

// .begin() i .end() or .top() for stack
// .empty() , .size() 
// .erase() for range or single element lists and vectors
// .push_back(), push_front()
// .swap() for swaping arrays

main(){
	
}
